import AuthState from "./AuthState";

const Navigation = () => {
  return (
    <nav className="h-32 bg-stone-900 flex items-center justify-center">
      <AuthState />
    </nav>
  );
};

export default Navigation;
